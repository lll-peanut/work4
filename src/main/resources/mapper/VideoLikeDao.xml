<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.peanut.Dao.VideoDao">
    <insert id="batchSaveOrUpdateVideoLike" parameterType="java.util.List">
        INSERT INTO video_like (id, video_id, user_id, is_cancel)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (
             #{item.id}, #{item.videoId}, #{item.userId}, #{item.isCancel}
            )
        </foreach>
        ON DUPLICATE KEY
        UPDATE
            is_cancel = VALUES(is_cancel)
    </insert>
</mapper>