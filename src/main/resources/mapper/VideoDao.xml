<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.peanut.dao.VideoDao">
    <select id="selectVideoListById" resultType="com.peanut.POJO.Video">
        SELECT `id`, `user_id`, `video_url`,`cover_url`,`title`,`description`,
        `like_count`,`comment_count`,`created_at`,`updated_at`,`deleted_at`
        from `video`
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND deleted = 0
    </select>

    <update id="batchUpdateVideoLikeCount">
        UPDATE video
        SET like_count = CASE
        <foreach collection="list" item="item" separator="">
            WHEN id = #{item.id} THEN like_count + #{item.totalIncrement}
        </foreach>
        END
        WHERE id IN
        <foreach collection="list" item="item" open="(" separator="," close=")">
            #{item.id}
        </foreach>
    </update>

    <select id="selectHomePageVideo" resultType="com.peanut.POJO.Video">
        SELECT `id`, `user_id`, `video_url`,`cover_url`,`title`,`description`,
        `like_count`,`comment_count`,`created_at`,`updated_at`,`deleted_at`
        from `video`
        <where>
            <if test="lastestTime != null">
                updated_at &lt; #{lastestTime}
            </if>
            AND deleted = 0
        </where>
        order by updated_at DESC
        limit #{size}
    </select>
</mapper>