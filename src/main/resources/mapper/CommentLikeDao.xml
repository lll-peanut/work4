<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.peanut.dao.CommentLikeDao">
    <insert id="batchSaveOrUpdateCommentLike" parameterType="java.util.List">
        INSERT INTO comment_like (id, comment_id, user_id, is_cancel)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (
            #{item.id}, #{item.commentId}, #{item.userId}, #{item.isCancel}
            )
        </foreach>
        ON DUPLICATE KEY
        UPDATE
        is_cancel = VALUES(is_cancel)
    </insert>


    <select id="batchQueryByUserAndComment" resultType="com.peanut.POJO.CommentLike">
        SELECT user_id AS userId,comment_id as commentId, is_cancel as isCancel
        FROM comment_like
        <where>
            <foreach collection="commentLikes" item="item" separator="OR">
                (
                user_id = #{item.userId} AND comment_id = #{item.commentId} AND is_cancel &lt;&gt; #{item.isCancel}
                )
            </foreach>
        </where>
    </select>
</mapper>